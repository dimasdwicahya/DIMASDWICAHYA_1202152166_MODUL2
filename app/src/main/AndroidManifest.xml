<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dimasdwicahya.dimasdwicahya_1202152166_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--untuk menambahkan tombol back-->
        <activity
            android:name=".DetailActivity"
            android:label="Detail Makanan"
            android:parentActivityName="com.example.dimasdwicahya.dimasdwicahya_1202152166_modul2.MainActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".MainActivity"/>
        </activity>

        <activity android:name=".MainMenu" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TakeAway" />




        <activity android:name=".DineIn"></activity>
    </application>

</manifest>